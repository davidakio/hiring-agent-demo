apiVersion: lumovra.dev/v1
tenant: hiring-agent-demo
systems:
  - name: Hiring Agent
    components:
      - name: hiring-prompt
        paths:
          - prompts/**
      - name: hiring-model
        paths:
          - config/model_config.yaml
      - name: hiring-eval
        paths:
          - eval/**
      - name: hiring-runtime
        paths:
          - src/**
